# Configuration cache: reuse the configuration phase across builds.
org.gradle.configuration-cache=true
# Version of the published library (MADR 0012). A release sets it to the tag's version
# without `-SNAPSHOT`; release.yml checks the two agree.
chatpaneVersion=0.1.0-SNAPSHOT
