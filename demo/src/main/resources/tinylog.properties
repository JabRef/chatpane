# Backend configuration for the demo (MADR 0005): the library logs through
# SLF4J, which slf4j-tinylog routes here.
level          = debug

writer         = console
writer.format  = {date: HH:mm:ss.SSS} {level} {class-name}.{method}(): {message}
